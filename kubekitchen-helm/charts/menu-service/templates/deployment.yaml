{{- if .Values.global.blueGreen }}
apiVersion: argoproj.io/v1alpha1
kind: Rollout
{{- else }}
apiVersion: apps/v1
kind: Deployment
{{- end }}
metadata:
  name: menu-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "menu-service.labels" . | nindent 4 }}
    tier: backend
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    {{- if .Values.global.blueGreen }}
    argocd.argoproj.io/sync-options: Replace=true
    {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "menu-service.selectorLabels" . | nindent 6 }}
  {{- if .Values.global.blueGreen }}
  strategy:
    blueGreen:
      activeService: menu-service-active
      previewService: menu-service-preview
      autoPromotionEnabled: false
      scaleDownDelaySeconds: 30
  {{- else }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  {{- end }}
  template:
    metadata:
      labels:
        {{- include "menu-service.selectorLabels" . | nindent 8 }}
        {{- include "menu-service.labels" . | nindent 8 }}
        tier: backend
      annotations:
        checksum/secret: {{ include "menu-service.secretChecksum" . }}
    spec:
      serviceAccountName: {{ .Values.global.serviceAccountName }}
      {{- if .Values.global.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.global.nodeSelector | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
      terminationGracePeriodSeconds: 30
      containers:
        - name: menu-service
          image: "{{ .Values.image.repository }}:{{ include "menu-service.imageTag" . }}"
          imagePullPolicy: {{ include "menu-service.imagePullPolicy" . }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
              name: http
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.service.targetPort | quote }}
            - name: NODE_ENV
              value: {{ .Values.global.nodeEnv | quote }}
            - name: JWT_EXPIRES_IN
              value: {{ .Values.global.jwtExpiresIn | quote }}
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: kubekitchen-secrets
                  key: MONGO_MENU_URI
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: kubekitchen-secrets
                  key: JWT_SECRET
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: {{ .Values.probes.liveness.port }}
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: {{ .Values.probes.readiness.port }}
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: {{ .Values.probes.startup.path }}
              port: {{ .Values.probes.startup.port }}
            initialDelaySeconds: {{ .Values.probes.startup.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}